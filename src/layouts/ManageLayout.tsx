import React, { FC } from 'react'
import { Layout, Spin } from 'antd'

const { Header, Content, Footer } = Layout

const ManageLayout: FC = () => {
  return <Layout>ManageLayout</Layout>
}

export default ManageLayout
