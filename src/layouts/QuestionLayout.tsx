import React, { FC } from 'react'
import { Layout, Spin } from 'antd'

const { Header, Content, Footer } = Layout

const QuestionLayout: FC = () => {
  return <Layout>QuestionLayout</Layout>
}

export default QuestionLayout
